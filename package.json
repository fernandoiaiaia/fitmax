{
  "name": "fitmax-workspace",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "@types/react-native": "^0.72.8",
    "turbo": "latest"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "packageManager": "npm@10.8.1"
}
